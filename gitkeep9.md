Отлично, давай я **сформулирую TASK 7 для Claude строго под UX**, в вашем новом файловом протоколе.
Это будет готовый `gitkeep8.md`, который ты просто кладёшь в `/tasks/inbox`.

---

## 🛰️ **D=>C (Dashka → Claude)**

### **TASK 8 — UX / MAP BEHAVIOR AUDIT & FIX**

**Источник истины:** GitHub
**Подключение:** `project_knowledge_search`
**Код НЕ писать без указания. Сначала анализ + UX-выводы.**

---

## 🎯 Цель задачи

Проверить и описать **первый пользовательский экран** и поведение карты:

* что именно видит пользователь **сразу при заходе**
* какая это страница по архитектуре (home / listing / map-only)
* выявить UX-баги и несоответствия ожиданиям
* дать **конкретные UX-fix предложения**

---

## 👁️ Сценарий пользователя (ОБЯЗАТЕЛЬНО)

### 1️⃣ Первый экран

При заходе пользователь видит:

* верхняя панель: **“SolarHousePrice”**
* карта (Mapbox)
* кластеры с цифрами
* легенду цен (CHF/m²) снизу

❓ **Вопросы для анализа:**

* Какая это страница по Next.js (`app/page.tsx`?)
* Это **listing page**, **map-only page** или **prototype screen**?
* Должен ли здесь быть список объектов (listings) или нет?

---

### 2️⃣ Поведение кластеров (КРИТИЧНО)

**Фактическое поведение:**

* нажимаю на зелёный кружок
* кластер **резко “улетает вверх”**
* карта ведёт себя неинтуитивно

❌ Это воспринимается как баг.

**Проанализировать:**

* используется ли `map.flyTo` / `easeTo`
* корректно ли считается `center`
* не применяется ли `offset` / `padding` от popup
* не конфликтует ли `fitBounds` + cluster click

---

### 3️⃣ Поведение одиночных точек

* что происходит при клике на **не кластер**, а точку?
* появляется ли popup?
* связана ли точка с:

  * listing?
  * price?
  * house detail?

---

### 4️⃣ Легенда цен (внизу)

* кликабельна ли?
* фильтрует ли карту?
* или это просто визуальная легенда?

❓ UX-вопрос:

> Пользователь ожидает, что нажатие на цвет = фильтр

---

## 🧠 Что нужно выдать в ответе (`gitreport8.md`)

**Формат СТРОГИЙ, без воды:**

```
C=>D

1. Что это за страница:
- Файл:
- Роль страницы:
- Это правильно / нет

2. Найденные UX-проблемы:
- [ ] Кластеры улетают вверх — причина
- [ ] Нет понятной реакции на клик точки
- [ ] Нет связи карта ↔ listings
- [ ] Легенда не интерактивна (если так)

3. Предлагаемые UX-решения:
- Кластеры: (конкретно что поменять)
- Точки: (popup / sidebar / hover)
- Легенда: (оставить / сделать фильтром)

4. Критичность:
- blocker / medium / cosmetic

Итог:
- ❌ UX не готов
- ⚠️ UX требует правок
- ✅ UX ок
```

---

## ❌ Запрещено в TASK 8

* ❌ писать код без команды
* ❌ чинить файлы самостоятельно
* ❌ менять архитектуру
* ❌ “предлагать абстрактно”

---

## ✅ Разрешено

* UX-анализ
* архитектурное объяснение
* конкретные инструкции:

  > «в этом файле сейчас происходит X, из-за Y»

---

## 🔥 Архитектурная подсказка (для Claude)

> Если это **первая точка контакта пользователя**,
> то UX важнее данных.
> Поведение карты должно быть **предсказуемым**.

---

Если хочешь — **следующим шагом (TASK 9)** мы сделаем:

* «идеальный UX сценарий» (как должен вести себя кластер → точка → объект)
* или подключим sidebar listings синхронно с картой

Готов зафиксировать TASK 8 как стандарт Solar UX Audit. 🚀







D=>C task7 отправлен Посмотри GITKEEP*.md и приступай. подключись через project_knowledge_search GitHub https://github.com/Solarpaletten/solar-nextjs-template 
