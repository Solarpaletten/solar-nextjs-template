🛰️ **D=>C (Dashka → Claude)**
Принято. Уточняю и **финально корректирую протокол**, чтобы он отражал именно то, что ты сейчас сформулировал. Ниже — **ГОТОВАЯ версия**, можно копировать как основу для всех проектов.

---

# ☀️ SOLAR FILE-DRIVEN EXECUTION PROTOCOL (FINAL)

## 📌 Ключевая идея (ВАЖНО)

**Мы не присылаем задачи в чате.
Вся ТЗ находится В ФАЙЛЕ в GitHub.**

`GITKEEP*.md` — это **контейнер задачи**, а не просто `.gitkeep`.

* `.gitkeep` — это **anchor-место**
* `GITKEEP*.md` — это **ТЗ + список anchor’ов**
* GitHub — **единственный источник истины**

---

## 🔐 Обязательное подключение

Ты **ОБЯЗАН** работать с GitHub **ТОЛЬКО** через:

```
project_knowledge_search
```

❌ Fetch / URL / web — запрещены.

---

## 🧠 Обязательное авто-подтверждение (БЕЗ НАПОМИНАНИЙ)

После подключения ты **САМ** обязан написать:

```
C=>D
Подключён к GitHub через project_knowledge_search.
Обнаружен файл: GITKEEP<номер>.md
ТЗ прочитано. Приступаю к исполнению.
```

Без этого шага работа **запрещена**.

---

## 📄 Что содержит `GITKEEP*.md`

Файл может содержать:

1. **ТЗ / пояснение от Dashka**
2. **Правила или ограничения**
3. **Список anchor’ов (`.gitkeep`)**

Пример структуры файла:

```md
# GITKEEP2 — API & UI Tasks

## Цель
Реализовать API и подготовить UI каркас.

## Ограничения
- не создавать новых папок
- переиспользовать код из _legacy

## Anchor-задачи
app/api/price/.gitkeep
components/listings/.gitkeep
prisma/migrations/.gitkeep
public/.gitkeep
```

---

## 🔁 Рабочий цикл (АВТОМАТИЧЕСКИЙ)

1. Ты находишь `GITKEEP*.md`
2. Читаешь ТЗ **из файла**
3. Пишешь подтверждение (см. выше)
4. Сам выбираешь первый anchor
5. Проверяешь переиспользование
6. Реализуешь код
7. Удаляешь соответствующий `.gitkeep`
8. Commit → Push
9. Переходишь к следующему anchor’у
10. Файл завершён → задача закрыта

---

## ❌ Запрещено

* ждать ТЗ в чате
* игнорировать текст в `GITKEEP*.md`
* придумывать архитектуру
* создавать новые папки
* работать без GitHub

---

## 🏁 Итоговая формула Solar

```
Файл = ТЗ
GITKEEP = задача
.gitkeep = anchor
Commit = маркер выполнения
```

---

📡 **Ожидаю автоматическое подтверждение после чтения следующего `GITKEEP*.md`.** 🚀


app/api/price/.gitkeep

omponents/listings/.gitkeep

prisma/migrations/.gitkeep

public/.gitkeep